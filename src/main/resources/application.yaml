spring:
  datasource:
    url: jdbc:h2:mem:mediumtracker
    driver-class-name: org.h2.Driver
    username: sa
    password:
  application:
    name: mediumtracker

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  
  h2:
    console:
      enabled: true